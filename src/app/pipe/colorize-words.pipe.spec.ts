import { ColorizeWordsPipe } from './colorize-words.pipe';

describe('ColorizeWordsPipe', () => {
  it('create an instance', () => {
    const pipe = new ColorizeWordsPipe();
    expect(pipe).toBeTruthy();
  });
});
